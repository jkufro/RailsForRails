
simplecov:
  stage: test
  image: docker.io/ruby:2.5.1
  script:
    - bunlde install
    - rails test
  artifacts:
    paths:
      - coverage/
  variables:
    GIT_SSL_NO_VERIFY: "true"

pages:
  stage: deploy
  dependencies:
    - simplecov
  script:
    - mv coverage/ public/
  artifacts:
    paths:
      - public
    expire_in: 30 days
