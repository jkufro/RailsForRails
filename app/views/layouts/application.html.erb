<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>RailsForRails</title>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <%= csrf_meta_tags %>
    </head>

    <body>
        <%= render :partial => "partials/nav" %>

        <main>
            <%= yield %>

            <%= render partial: 'partials/templates' %>
            <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
            <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

            <% if !logged_in? %>
                <%= javascript_include_tag 'vue_login', 'data-turbolinks-track': 'reload' %>
            <% elsif is_admin? %>
                <%= javascript_include_tag 'vue_admin', 'data-turbolinks-track': 'reload' %>
            <% else %>
                <%= javascript_include_tag 'vue_main', 'data-turbolinks-track': 'reload' %>
            <% end %>
        </main>

        <%= render :partial => "partials/footer" %>
    </body>
</html>
